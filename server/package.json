{
  "name": "code-midi-server",
  "version": "0.1.0",
  "description": "server for code-midi to interface with midi devices",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "easymidi": "^2.0.1",
    "ws": "^7.4.0"
  }
}
